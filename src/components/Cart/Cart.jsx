

const Cart = ({selectedActor}) => {
    const {name} = selectedActor;
    
    
   
    return (
        <div>
            
          <h1>{name}</h1>
          
        </div>
    );
};

export default Cart;